import 'package:MediLink/ammar/lib/core/constant/imageasset.dart';
import 'package:flutter/material.dart';

class Photoforgetpasssword extends StatelessWidget {
  const Photoforgetpasssword({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        height: 165.47,
        width: 202.7,
        child: Image.asset(
          AppImageAsset.forgetpassword,
        ));
  }
}
